export enum ERoute {
  HOME = '/',
  BLOG = '/blog',
  OFFERS = '/offers',
  EVENTS = '/events',
  MYSTERY = '/mystery',
}

export enum EBreakpoints {
  MOBILE = 375,
  TABLET = 768,
  LAPTOP = 1024,
  DESKTOP = 1170,
}
